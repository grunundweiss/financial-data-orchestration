apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    # Pinned so the committed dashboard JSON can reference it by uid.
    uid: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
